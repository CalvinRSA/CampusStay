# === CORE FRAMEWORK ===
fastapi==0.115.0
uvicorn[standard]==0.30.6

# === DATABASE ===
SQLAlchemy==2.0.35          # ← CRITICAL: 2.0.36 crashes on Railway with Python 3.13
psycopg2-binary==2.9.9
alembic==1.13.2

# === SECURITY & AUTH ===
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9     # Required for file uploads (Form + UploadFile)

# === DATA VALIDATION (Pydantic v2 - safe versions) ===
pydantic==2.9.2
pydantic-core==2.23.4
email-validator==2.2.0      # For EmailStr in schemas

# === CLOUD STORAGE (Cloudflare R2 / Backblaze B2 compatible) ===
boto3==1.35.24
botocore==1.35.24

# === UTILITIES ===
python-dotenv==1.0.1