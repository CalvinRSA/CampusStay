# === CORE FRAMEWORK (latest stable) ===
fastapi==0.115.0
uvicorn[standard]==0.30.6

# === DATABASE (CRITICAL FIXES FOR PYTHON 3.13 + RENDER) ===
SQLAlchemy==2.0.35               # 2.0.36+ crashes on Render with Python 3.13
psycopg[binary]==3.2.3            # NEW OFFICIAL DRIVER — replaces psycopg2-binary

# === AUTH & SECURITY ===
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9           # Required for file uploads

# === DATA & VALIDATION (Pydantic v2 stable) ===
pydantic==2.9.2
pydantic-core==2.23.4
email-validator==2.2.0

# === CLOUD STORAGE (Cloudflare R2 / Backblaze B2) ===
boto3==1.35.24
botocore==1.35.24

# === MIGRATIONS & UTILITIES ===
alembic==1.13.2
python-dotenv==1.0.1